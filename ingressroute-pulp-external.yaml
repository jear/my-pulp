apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: pulp-external
  namespace: pulp
spec:
  entryPoints:
  - websecure
  routes:
  - kind: Rule
    match: Host(`pulp.83-206-89-105.nip.io`)
    middlewares:
#    - name: security
#    - name: ratelimit
    services:
    - name: pulp-web-svc
      port: 24880
  tls:
    certResolver: letsencrypt
